const profile = require('./module/1-module')
const show = require('./module/2-function')

const server = require('./server/os')

// console.log(profile) 

show(profile.age)
// show(name)
// show(age)
// show(addr)